export interface STUDENT {
    logo: string; // URL to the student's logo image
    name: string; // Name of the student
    id: string;   // Unique identifier for the student
    detailsUrl: string; // URL to the student's profile page
  }
  